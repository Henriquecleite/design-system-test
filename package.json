{
  "name": "root",
  "scripts": {
    "build": "lerna exec --scope select -- babel index.js --out-dir dist --config-file ../../babel.config.json",
    "build-watcher": "node ./scripts/build-watcher.js",
    "dev": "lerna run --stream --scope docs dev"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/polyfill": "^7.11.5",
    "@babel/preset-env": "^7.11.5",
    "@babel/preset-react": "^7.10.4",
    "chokidar": "^3.4.2"
  }
}